#include<stdio.h>
#include<windows.h>
#include<conio.h>

int main()
{
 char str[100];
 printf("Ingrese una cadena de numeros: ");
 printf( "\rLength is: %d", printf("Entered string is: %s\n", gets(str)) - 20);
 return 0;
}